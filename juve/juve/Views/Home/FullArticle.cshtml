@model juve.Models.News
@*@model juve.Models.Coment*@
@{
    ViewBag.Title = Model.Title;
}

@*<div>
    <h1>@Model.Title</h1>
    <p>@Model.Text</p>
</div>*@

<section class="news floatfix">



    <img src="~/Content/Images/juve-logo.gif" />
    <h1>@Model.Title</h1>
    <p>@Model.Text</p>
   
    @Html.Partial("_AddComentPartial")

    <article>
        @foreach (var item in Model.Coments)
        {
            <div>
                <div>
                    @item.User.Email
                </div>
                <div>
                    @item.Text
                </div>

            </div>
        }
    </article>


</section>